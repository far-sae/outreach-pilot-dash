# Normalize line endings to LF in the repo and on checkout, so Prettier/ESLint
# behave identically on Windows, macOS, and Linux.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
